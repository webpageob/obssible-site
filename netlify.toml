# Netlify runs build.py on every push, then serves the generated files.
#
# This is what makes browser-based publishing possible: a CMS can only commit
# markdown, it cannot run Python. Without this, saved posts would never appear.

[build]
  command = "pip install -r requirements.txt && python3 build.py"
  publish = "."
